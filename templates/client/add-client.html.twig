{# templates/client/add-client.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}Add New Client{% endblock %}

{% block body %}
    <div class="col-xl-4 col-lg-5 offset-xl-2 offset-lg-1">
        <div class="booking-form">
            <h3>Booking Your Hotel</h3>
            {{ form_start(form, {'attr': {'class': 'form'}}) }}
            <div class="form-group">
                {{ form_label(form.firstname, 'Firstname:', {'label_attr': {'class': 'control-label'}}) }}
                {{ form_widget(form.firstname,{'attr': {'class': 'form-control date-input'}}) }}
                <i class="icon_calendar"></i>
            </div>
            <div class="form-group">
                {{ form_label(form.lastname, 'Lastname:', {'label_attr': {'class': 'control-label'}}) }}
                {{ form_widget(form.lastname,{'attr': {'class': 'form-control date-input'}}) }}
                <i class="icon_calendar"></i>
            </div>
            <div class="form-group">
                {{ form_label(form.email, 'Email:', {'label_attr': {'class': 'control-label'}}) }}
                {{ form_widget(form.email, {'attr': {'class': 'form-control date-input'}}) }}
                <i class="icon_calendar"></i>
            </div>
            <div class="form-group">
                {{ form_label(form.phone, 'Phone:', {'label_attr': {'class': 'control-label'}}) }}
                {{ form_widget(form.phone, {'attr': {'class': 'form-control', 'placeholder': 'Add phone number'}}) }}
            </div>
            <div>
                Payment Mode:
                <select name="payment_mode">
                    <option value="cash">Cash</option>
                    <option value="credit card">Credit Card</option>
                    <option value="visa">Visa</option>
                </select>
            </div>


            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
